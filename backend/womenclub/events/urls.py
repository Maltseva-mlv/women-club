# urls.py
from django.urls import path
from .views import get_events_for_date

urlpatterns = [
    # path('api/events/', get_events_for_date, name='get_events_for_date'),
    # Другие URL-паттерны вашего приложения
]
